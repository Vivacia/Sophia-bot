import discord
import random

    if "!battleship" in message.content.lower() or "battleship" in message.content.lower() and ("lets play" in message.content.lower() or "let's play" in message.content.lower()):
        battleship()